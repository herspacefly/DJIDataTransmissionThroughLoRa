# 甲烷浓度无人机无线远距离传输 —— 项目背景说明

> 本文档供 AI 助手（如 Codex、Claude 等）快速理解项目全貌。
> 阅读后应能判断：用户要做什么、已做到哪一步、有哪些硬性约束不能违反。

---

## 一、一句话目标

**让无人机在飞行中采集的甲烷浓度数据，实时、无线、远距离地传回地面电脑。**

---

## 二、业务背景

- 使用大疆 **Matrice 400（M400）** 行业无人机搭载气体监测设备，在空中采集甲烷浓度。
- 采集设备为 **Aeris Technology 公司的 Mira Pico** 气体监测设备。
- 应用场景为野外/远距离巡检，因此地面端与无人机之间**不可能靠 WiFi 或有线连接**，需要一条独立的远距离无线数据链路。
- 数据量极小：甲烷浓度 + 位置 + 时间戳，**每秒几十字节**即可满足需求，2Hz 更新频率已足够。

---

## 三、硬件清单与连接拓扑

### 硬件清单

| 设备 | 说明 |
|---|---|
| 大疆 Matrice 400（M400） | 行业无人机平台 |
| 大疆 妙算 3（Manifold 3） | 机载算力模块，运行 Ubuntu 20.04，通过 E-Port V2 装在飞机上 |
| 绿联 USB-C 拓展坞 | 插在妙算 3 唯一的 USB-C 口上，用于扩展外设 |
| USB 无线网卡 | 插在拓展坞上，用于让妙算 3 连上 Pico 的 WiFi |
| Aeris Mira Pico | 甲烷气体监测设备，自带 WiFi 热点 |
| 亿佰特 E28-2G4TBM-01 测试套件 ×2 | 2.4GHz LoRa 无线串口模块评估板（板载 E28-2G4T20SX），一块机载、一块地面 |
| 地面电脑 | Windows，用于接收数据 |

### 连接拓扑

```
【空中 - 无人机端】

  Mira Pico ──(WiFi)──> 妙算3的USB无线网卡
                              │
                              ↓
                          妙算 3 (Ubuntu 20.04)
                              │
                              │ USB-C → 绿联拓展坞
                              │              ├── USB无线网卡（连Pico）
                              │              └── E28 模块（/dev/ttyUSB0）
                              ↓
                        E28 机载端 ──┐
                                     │
                                     │  2.4GHz LoRa 无线
                                     │  （点对点广播，透明传输）
                                     │
                        E28 地面端 ──┘
                              │
                              │ USB (虚拟COM口)
                              ↓
【地面】              地面电脑 (Windows)
```

### 已实现的数据链路（Pico → 妙算 3）

1. 妙算 3 通过拓展坞上的 USB 无线网卡，连上 Mira Pico 的 WiFi 热点
2. 妙算 3 通过 **SSH** 从 Pico 取回甲烷浓度数据
3. **这一段已经跑通，不是当前要解决的问题**

### 待实现的数据链路（妙算 3 → 地面电脑）★本项目核心

即上图中 E28 机载端 → 2.4GHz 无线 → E28 地面端 → 电脑 这一段。

---

## 四、选定的技术方案：亿佰特 E28-2G4T20SX

### 为什么选它

- UART 无线串口模块，对上层软件完全透明（透明传输），Python 里就当普通串口读写
- 标称参考距离 6km（空旷、5dBi 天线、2.5m 高、1kbps 空速条件下）
- 发射功率 20dBm，灵敏度 -134dBm，ISM 免许可频段

### 关键参数（出厂默认值，测试中未修改）

| 参数 | 值 |
|---|---|
| 工作频段 | 2400–2500 MHz |
| 默认信道 | 0x13 = **2413 MHz** |
| 串口波特率 | **9600**（8N1，无流控） |
| 空中速率 | **10 kbps** |
| 模块地址 | 0x0000 |
| 发射功率 | 20 dBm（可调 20/17/14/10） |
| 调制方式 | LoRa / FLRC / GFSK |
| 工作模式 | 模式 0（透明传输） |
| 单帧上限 | **121 字节**（普通模式） |
| 缓冲区 | 2048 字节 |
| 发射电流 | 160 mA @5V |
| 通信电平 | **3.3V TTL**（接 5V 会烧毁） |

### ⚠️ LoRa 是点对点的

**必须成对使用。** 一块板只能做一端，需要完全相同参数的两块才能互通。

### 模块配置命令（HEX 格式，PuTTY 等纯文本终端发不了）

| 命令 | 作用 |
|---|---|
| `C1 C1 C1` | 读取当前参数（返回 6 字节，如 `C0 00 00 13 18 04`） |
| `C0` + 5 字节参数 | 写入参数并掉电保存 |
| `C2` + 5 字节参数 | 写入参数，不保存 |
| `C3 C3 C3` | 读版本号 |
| `C4 C4 C4` | 复位模块 |
| `E2 E2 E2` / `E3 E3 E3` | 透传模式下开 10 秒配置窗口 |

---

## 五、妙算 3（机载端）环境

| 项目 | 值 |
|---|---|
| 系统 | Ubuntu 20.04，Linux Kernel 5.10 |
| SDK | JetPack 5.1.3，DJI PSDK 3.12 |
| Python | 3.8（`python3`） |
| SSH 账号 | `dji` / `dji` |
| **权限** | **无 root**，装包必须用 `pip3 install --user` |
| USB-C 口 | **只有一个**，默认 5V/2A 输出，所有外设都靠拓展坞扩展 |

### SSH 地址由 E-Port 接口决定

| E-Port 口 | SSH 地址 |
|---|---|
| E1 | 192.168.42.120 |
| E2 | 192.168.42.130 |
| E3（推荐） | **192.168.42.140** |

> ⚠️ `192.168.42.x` 网段**依赖 PC 通过 USB 线直连飞机调参口**建立的 USB 虚拟网卡。
> 如果妙算 3 改为接 WiFi/以太网，则 IP 由路由器分配，该网段失效。
> 开机自启动可用 `systemd --user`（无 root 权限）。

---

## 六、已完成的工作（时间线）

| 步骤 | 状态 |
|---|---|
| Pico → 妙算 3 数据获取（WiFi + SSH） | ✅ 早前已完成 |
| 安装 CH340/CP210x 驱动（Windows 地面端） | ✅ 免装，系统自带 |
| 两块 E28 板分别在两台电脑上通过 PuTTY 互发 "HELLO" | ✅ **成功** |
| 排查妙算 3 SSH 连不上（192.168.42.140 超时） | ✅ 定位为 USB 调参线/供电/网段问题，已解决 |
| 妙算 3 识别 E28 模块 | ✅ `ls /dev/ttyUSB*` 显示 `/dev/ttyUSB0` |
| 确认串口属组 | 已查：`crw-rw---- 1 root dialout`，需确认 dji 用户是否在 dialout 组 |
| **无人机开机干扰测试** | ⏳ **下一步** |

### PuTTY 配置要点（地面端已验证可用）

- Connection type: **Serial**
- Serial line: 对应 COM 口；Speed: **9600**
- Connection → Serial: 8 / 1 / None / Flow control **None**
- Terminal → Local echo: **Force on**（否则打字不显示）
- `dmesg` 在妙算 3 上不可用（`Operation not permitted`，无 root），改用 `lsusb` 查设备

---

## 七、已知风险与限制 ★重要

### 1. 2.4GHz 频段与 M400 图传冲突（最大风险）

- M400 的 **O4 图传行业增强版工作频段包含 2.400–2.4835 GHz**
- E28 工作频段 2400–2500 MHz，默认信道 **2413 MHz 正落在图传带内**
- 同机载 + 同频段 + 图传功率远高于 20dBm，可能导致机载端 LoRa 被阻塞

**缓解手段（按优先级）：**
1. 在 DJI Pilot 2 中把图传**手动锁定到 5.8GHz**，把 2.4G 让给 LoRa
2. 更换 E28 信道，避开图传常用频点
3. 开启 LBT（发前侦听信道）
4. 机载天线远离 O4 天线和碳板，最好伸出机身、朝下安装

### 2. 妙算 3 的 USB-C 高压输出禁止开启

`DjiPowerManagement_OutputHighPower` 可输出 14V/5A，**会烧毁 USB 外设**（含 E28 板和 WiFi 网卡）。严禁调用。

### 3. 供电预算

USB-C 默认对外 5V/2A，拓展坞上挂 WiFi 网卡 + E28（发射瞬时 160mA），需留意 over-current 掉口。

### 4. 天线必须先接再上电

不接天线发射会导致反射功率烧毁功放。**顺序：拧好天线 → 插 USB；换天线 → 先断电。**
功放烧毁是隐性的（模块仍能识别、仍能接收，只是发不出去）。

### 5. 两块模块距离不能太近

小于 1 米会因接收机饱和出现"近距堵死"。**测试时保持 2~3 米以上。**

### 6. 环境干扰源

- **USB 3.0 接口**：其扩频时钟噪声落在 2.4GHz，会抬高接收底噪 → **优先用黑色 USB 2.0 口或加延长线**
- 金属桌面/机壳、天线附近有金属物体会严重衰减信号
- 两天线极化方向需一致（都竖直朝上）

---

## 八、当前状态

**无线链路本身已验证可用**（两台电脑 + PuTTY 互发 HELLO 成功）。
**妙算 3 已能识别 E28 模块**（`/dev/ttyUSB0` 存在）。

尚未验证的是：

1. 妙算 3 上的程序能否正常读写该串口（权限问题）
2. **无人机开机、图传工作时，2.4GHz 干扰到底有多严重** ← 决定方案可行性的关键

---

## 九、下一步计划

### 阶段 1：干扰测试（当前）

编写两个 Python 脚本：

- `beacon_tx.py`（跑在妙算 3）：按序号持续发送测试帧，2Hz，默认 60 秒
  - 帧格式示例：`#0001,1695541200.12\n`，**单帧 ≤ 40 字节**
- `beacon_rx.py`（跑在 Windows 电脑）：监听 COM 口，统计丢包率、最大连续丢包段，存 CSV

**测试方法（对比法）：**
1. **基线**：飞机不开机（或妙算 3 独立供电），测一次丢包率 → 应接近 0%
2. **干扰**：飞机上电、遥控器与图传正常工作（不解锁不起飞），再测一次
3. 对比判读：

| 丢包率 | 判断 | 处理 |
|---|---|---|
| 开机前后均 ~0% | 干扰可忽略 | 直接进入开发阶段 |
| 开机后 10~30% | 有干扰但可用 | 图传锁 5.8GHz 后重测 |
| 开机后 >50% | 图传压制严重 | 锁 5.8G + 换信道 + 挪天线；仍不行需更换方案 |

4. 可选：飞机开机状态下，对比机载天线不同安装位置（贴机身 / 伸出机臂 / 朝下），选最优

> 测试时务必让图传真正工作（遥控器已连、画面在跑），否则结果偏乐观。

### 阶段 2：正式程序开发

1. **机载端转发程序**（妙算 3）：从 Pico 取甲烷浓度 → 封装短帧 → 串口发送（2Hz）
   - 建议帧格式：`$CH4,浓度,纬度,经度,高度,时间戳\n`
   - 要求：串口断开自动重连、异常不崩、日志记录
2. **地面端接收程序**（Windows）：解析帧 → 实时显示 → 存 CSV → 画实时曲线
3. **开机自启动**：`systemd --user` 配置

### 备选方案（若 2.4GHz 干扰无法解决）

妙算 3 的 PSDK 本身就提供：
- **MOP 通道**：妙算 3 与遥控器高速互通
- **Pilot 上云通道**：WebSocket 向第三方云端推数据

走 O4 图传链路是零硬件成本、零频段冲突的方案，甲烷数据体量小，完全够用。
建议 LoRa 作为**独立于图传的备份链路**，而非唯一链路。

---

## 十、给 AI 助手的硬性约束 ★违反会导致跑不通

1. **波特率固定 9600，不要提高。** 空中速率是 10kbps（约 1250 字节/秒），串口 9600（约 960 字节/秒）刚好匹配。若串口提到 115200 而空速不变，数据进得来发不出去，2048 字节缓冲区瞬间溢出，会疯狂丢包并误导排查方向。
   - 若确实需要提速，**串口波特率与空中速率必须同步提升**。
2. **单帧不超过 121 字节。** 不要发 JSON，用短 ASCII 帧。
3. **无 root 权限。** Python 包用 `pip3 install --user`；系统配置用 `systemd --user`。
4. **目标平台是 aarch64 Ubuntu 20.04 / Python 3.8**，代码需兼容。
5. **不要设计需要 ACK 握手的协议。** LoRa 透明传输是广播式，无确认重传机制；丢包统计只能靠序号连续性。
6. **严禁调用妙算 3 的高压输出接口。**
7. 数据帧建议自带**序号和时间戳**，便于丢包统计与事后分析。

---

## 附录：常用命令速查

### 妙算 3（机载端）

```bash
ssh dji@192.168.42.140              # 登录（E3 口）

ls /dev/ttyUSB*                     # 查看串口设备
ls -l /dev/ttyUSB*                  # 查看权限与属组
lsusb                               # 查 USB 设备（dmesg 无权限，用这个）
groups                              # 确认是否在 dialout 组
sudo usermod -aG dialout dji        # 加入 dialout 组（需重新登录生效）

python3 --version                   # 应为 3.8
pip3 install --user pyserial        # 装串口库
python3 -c "import serial; print(serial.__version__)"

echo "HELLO" > /dev/ttyUSB0         # 最简端到端验证（不写 Python 也能测）
```

### Windows（地面端）

```powershell
ipconfig | findstr "192.168.42"     # 确认 USB 虚拟网卡网段是否存在
```

- 设备管理器 → 端口 (COM 和 LPT) 查看 COM 口号
- PuTTY：Serial + COM 口号 + 9600 + Local echo Force on
