# 甲烷浓度无人机无线远距离传输 —— 开发日志

> 记录从方案调研到地面站上线的完整开发过程，重点记录三次方案级大改与各类踩坑的解决方案。
> 配套文档：《甲烷监测无人机无线传输项目背景说明.md》（项目背景与硬件规格）

---

## 零、项目目标

**让无人机在飞行中采集的甲烷浓度数据，实时、无线、远距离地传回地面电脑。**

| 项目 | 内容 |
|---|---|
| 无人机 | 大疆 Matrice 400（M400） |
| 机载算力 | 大疆 妙算 3（Manifold 3，Ubuntu 20.04 / aarch64） |
| 采集设备 | Aeris Technology **Mira Pico** 甲烷气体监测仪 |
| 无线链路 | 亿佰特 **E28-2G4T20SX**（2.4GHz LoRa 无线串口）× 2 |
| 地面端 | Windows 电脑 + 自研地面站 |

数据量极小（甲烷浓度 + 时间戳，1Hz），但要求远距离、独立于图传。

---

## 一、时间线总览

| 阶段 | 内容 | 结果 |
|---|---|---|
| 1 | 方案调研与选型 | 选定 E28-2G4T20SX |
| 2 | 环境准备：驱动、PuTTY | 免装驱动，PuTTY 直连成功 |
| 3 | 双机互发 HELLO | ✅ 无线链路打通 |
| 4 | **妙算 3 SSH 连不上** | 定位为 USB 虚拟网卡缺失 |
| 5 | **妙算 3 损坏，方案大改** | 改为 E28 直挂 Pico |
| 6 | **端口识别混乱** | 发现 ttyUSB0 是 Pico 数据口而非 LoRa |
| 7 | **端口漂移** | udev 规则绑定固定别名 |
| 8 | 数据量评估 | 134 字节超 121 上限 → 重组短帧 |
| 9 | 地面站开发 | 替代 PuTTY 的专用 GUI |
| 10 | 远程控制 | 终端标签页，避免直接关机 |
| 11 | 工程化 | 开机自启、CSV 轮转、布局修复 |

---

## 二、阶段 1：方案调研与选型

### 需求拆解
- Pico → 妙算 3 的数据获取**已实现**（WiFi + SSH），不在本次范围
- 本次要解决的是 **妙算 3 → 地面电脑** 这一段
- 数据量约几十字节/秒，2Hz 足够

### 选型结论：亿佰特 E28-2G4T20SX
理由：UART 透明传输对上层完全透明、标称 6km、ISM 免许可频段。

### 关键认知：LoRa 必须成对使用
用户最初只买了一块"测试套件"，但 LoRa 是点对点的，**一块只能做一端**。必须再补一块，参数一致才能互通。这是选型阶段最重要的一次纠偏。

### 已识别的最大风险（贯穿全程）
**M400 的 O4 图传工作频段包含 2.400–2.4835 GHz，与 E28 的 2400–2500 MHz 完全重叠**，默认信道 2413 MHz 正落在图传带内。

缓解手段（按优先级）：
1. Pilot 2 中把图传锁定到 5.8GHz
2. 更换 E28 信道
3. 开启 LBT（发前侦听）
4. 机载天线远离 O4 天线与碳板

---

## 三、阶段 2：环境准备

### 3.1 驱动：最后一行都没装
原以为要装 CH340/CP210x 驱动，实际 **Windows 10/11 自带**。

正确做法：**先插再看设备管理器**，不要预先装驱动。判断依据：
- 设备管理器 → 端口 (COM 和 LPT) 出现 COM 口 = 驱动就绪
- 出现黄色感叹号才需要装，且必须按硬件 ID 对号入座
  - `VID_1A86&PID_7523` → CH340（沁恒官网 CH341SER）
  - `VID_10C4&PID_EA60` → CP210x（Silicon Labs 官网）

### 3.2 串口调试助手报毒事件
亿佰特官网挂的 XCOM 下载被杀软拦截。

**决策：不装，且明确建议不要为了装它去关杀毒软件或加白名单。**

替代方案：
- 最终主力：**Python + pyserial**（调试代码直接变成产品代码，且能发 HEX 字节）
- 图形界面备选：PuTTY（从作者官网下，不从第三方下载站）

> 关键认知：E28 的参数配置命令是 HEX 字节（`C1 C1 C1` 读参数），**PuTTY 这类纯文本终端发不了**。这是后面全面转向 Python 的伏笔。

### 3.3 硬件操作规范
- **必须先拧天线，再插 USB**。不接天线发射，反射功率会烧功放
- 功放烧毁是**隐性的**：模块仍能识别、仍能接收，只是发不出去
- 两块板距离不能小于 1 米（接收机饱和导致"近距堵死"），建议 2~3 米起测

---

## 四、阶段 3：首次通信成功

两台电脑各插一块 E28，PuTTY 配置：
- Connection type: **Serial**
- Serial line: 对应 COM 口；Speed: 9600
- Connection → Serial: 8 / 1 / None / Flow control **None**
- Terminal → **Local echo: Force on**（否则打字不显示，会以为键盘坏了）

**互发 HELLO 成功**，无线链路本身得到验证。

---

## 五、【困难 1】妙算 3 SSH 连接超时

### 现象
```
ssh dji@192.168.42.140
ssh: connect to host 192.168.42.140 port 22: Connection timed out
```
之前能连，突然不行。

### 定位过程
关键判断：`ipconfig | findstr "192.168.42"` **无任何输出**。

这证明 `192.168.42.x` 是 **PC 通过 USB 线直连飞机调参口**建立的 USB 虚拟网卡网段，与 WiFi、图传无关。超时 = 物理链路断了，不是密码问题。

### 原因清单（按顺序）
1. 飞机未开机（妙算 3 靠 E-Port V2 从飞机取电）
2. PC 与调参口之间的 USB 线未插，或**线只能充电不能传数据**
3. 妙算 3 换了 E-Port 口（E1=`.120` / E2=`.130` / E3=`.140`）
4. 拓展坞新接了 PD 供电或网卡 → 网络路径切换到局域网，该网段失效
5. 妙算 3 冷启动需 1~2 分钟（绿灯常亮=启动中，慢闪=就绪）
6. 驱动问题（需装 DJI Assistant 2）

### 解决
最终定位为**连接链路问题**（线/供电），并按正确顺序恢复：**先开飞机 → 等灯变慢闪 → 最后插调参线**（反过来插线开机容易握手失败）。

---

## 六、【方案大改 1】妙算 3 损坏 —— 拓扑重构

### 触发
调试过程中妙算 3 无法使用。

### 方案变更

**原拓扑：**
```
Pico --(WiFi/SSH)--> 妙算3 --(USB)--> E28机载端 ~~> E28地面端 --> 电脑
```

**新拓扑：**
```
Pico --(自身运行程序)--> E28机载端 ~~> E28地面端 --> 电脑
```

### 决策依据
关键发现：**Mira Pico 本身就是一台 Linux 电脑**（Debian），有 SSH、有 Python、有 USB 主机模式。妙算 3 在项目中的角色本质只是"会跑程序的中间节点"，这个角色 Pico 自己就能承担。

学术界已有 **Raspberry Pi 4 + MIRA Pico** 挂在无人机上的实证方案，思路一致。

### 验证三关（决定是否可行）
1. **Pico 能否识别 E28**（USB 主机模式）→ `lsusb` / `ls /dev/ttyUSB*`
2. **Pico 上能否跑 Python + pyserial** → 发现 `/usr/local/bin/pyserial-miniterm` 已存在，**直接过关**
3. **数据从 Pico 内部怎么取** → 探索数据源

### 排除的错误方向
曾考虑用 Pico 的 **RS-232 输出**。但 RS-232 是 ±12V 电平，E28 是 3.3V TTL，**直连会烧毁**。需电平转换模块 + 裸模块，**硬件改动大，放弃**。

> 后续澄清：软件读写 `/dev/ttyUSB_RS232` 走的是 USB 协议，不碰物理电平，是安全的。名字里带 RS232 只是厂商命名习惯。

---

## 七、【困难 2】端口识别混乱 —— ttyUSB0 不是 LoRa

### 现象
妙算 3 上 `ls /dev/ttyUSB*` 只显示 `ttyUSB0`，一度以为 E28 没被识别。

### 关键洞察
Pico 上执行后出现了：
```
/dev/ttyUSB0
/dev/ttyUSB1
/dev/ttyUSB2
/dev/ttyUSB_AERIS -> ttyUSB1
/dev/ttyUSB_RS232 -> ttyUSB0
```

**`ttyUSB_AERIS`、`ttyUSB_RS232` 是厂商用 udev 规则写的固定别名，只会给自家设备起名字。** 第三方 LoRa 板不会有别名，所以**没有别名的那个"裸号"就是 LoRa**。

### 反转：数据源是 RS232 口而非 AERIS 口
用 `pyserial-miniterm /dev/ttyUSB0 9600` 打开时，Pico 的采集数据自己流出来了——证明 **ttyUSB0（即 `ttyUSB_RS232`）才是数据输出口**，不是 LoRa。

**此前给用户的建议（用 `ttyUSB_AERIS` 作源）错误，已纠正。**

### 最终映射
| 角色 | 设备 | 说明 |
|---|---|---|
| 数据源 SRC | `/dev/ttyUSB_RS232` | Pico 甲烷数据输出，9600 |
| 发射口 DST | `/dev/ttyUSB2`（后改为固定别名） | 无别名者即 LoRa |

### 教训
不要凭名字猜测，`ttyUSB_AERIS` 听起来最像数据口，实际出数据的是 `ttyUSB_RS232`。**验证靠 miniterm 直接看输出，而不是靠推理。**

---

## 八、【困难 3】端口漂移 —— udev 固定别名

### 现象
调试中数据突然中断，一度怀疑 Pico 故障。

### 定位
**端口号按插入顺序分配，设备重新插拔后编号会重排。** 数据从 ttyUSB0 漂到了 ttyUSB1。

### 判断依据（排除硬件故障）
- 仍能 SSH、lsusb 能看到设备 → 系统活着
- "突然没数据"恰恰说明**不是烧毁**（真烧了会直接不识别）
- 用 Web 界面（`:1880` Node-RED / `:3000` / `:8000`）交叉验证 → 仪器仍在出数

### 解决：udev 规则绑定固定名
```bash
sudo nano /etc/udev/rules.d/99-lora.rules
```
```
SUBSYSTEM=="tty", ATTRS{idVendor}=="1a86", ATTRS{idProduct}=="7523", SYMLINK+="ttyUSB_LORA"
```
```bash
sudo udevadm control --reload-rules && sudo udevadm trigger
```

**此后所有命令一律用别名，不再用 ttyUSB0/1/2。**

### 附带排查经验
串口突然无数据的四类原因：
1. 端口被其他进程占用（`pkill -f miniterm`）
2. **端口漂移**（最常见）
3. Pico 停止测量（用 Web 界面交叉验证）
4. `stty` 波特率被改

---

## 九、【困难 4】数据量超限 —— 重组短帧

### 测量结果
```
rate    = 139 字节/秒
line    = 1.0 行/秒
maxline = 134 字节
```

### 问题
**134 > 121**，LoRa 单帧硬上限。长行会撑爆缓冲区导致疯狂丢包。

速率 139 B/s 远低于空中速率 10kbps（约 1250 B/s），**速率本身没问题，问题在单帧长度**。

### 解决：提取指定列重组短帧
用户需要的字段：第 1 列 Time Stamp、第 7 列 CH4、第 8 列 H2O、第 9 列 C2H6（末两列 No Data 为无用数据）。

```python
IDX_TS, IDX_CH4, IDX_H2O, IDX_C2H6 = 0, 6, 7, 8   # 0 起索引
```

**并且加入序号**（这是关键改进）：
```
$MIRA,<序号>,<时间戳>,<CH4>,<H2O>,<C2H6>
```

**为什么要加序号**：LoRa 是广播式透明传输，**没有 ACK 确认机制**，无法知道丢没丢。有了序号，地面站才能算「应到 100 帧、实到 87 帧 → 丢包 13%」。

> **这个丢包率指标是后续干扰测试的核心判据** —— 飞机开机前后各测一次对比，即可量化 M400 图传的干扰程度。

效果：单帧从 134 字节降到约 45 字节，带宽省三分之二，且彻底避开 121 上限。

### 排除的错误方向
**波特率绝对不能提高。** 空中速率 10kbps，串口 9600 刚好匹配。若串口提到 115200 而空速不变，数据进得来发不出去，2048 字节缓冲区瞬间溢出——会疯狂丢包并**误导排查方向**（误以为是干扰）。

---

## 十、阶段 9：地面站开发（替代 PuTTY）

用户需求："想做个类似 PuTTY 的软件，只不过是专门用来传输数据的。"

### 为什么不用 PuTTY
- 是通用终端，看数值/画曲线/存表格/算丢包**一个都做不到**
- 纯文本终端，**发不了 HEX 字节**

### 地面站功能演进

**v1：基础版**
- 串口下拉自动扫描（`serial.tools.list_ports.comports()`）
- CH4/H2O/C2H6 数值卡
- 单张 CH4 曲线
- 自动存 CSV 到桌面
- 丢包率统计
- 原始数据日志

**v2：用户定制**
- **三条独立曲线**（CH4 红 / H2O 蓝 / C2H6 绿）
  - 刻意做成三张子图而非一张：三个量级差异大（CH4 约 2ppm，C2H6 为 ppb 级，H2O 可达上千 ppm），画一起小数值会被压成直线
  - 单位修正：**CH4 = ppm，H2O = ppm，C2H6 = ppb**（此前误将 H2O 写成 %）
- **CSV 存到程序目录下 `data/` 子文件夹**，每 **30 分钟**自动轮转新文件
  - 文件名带起始时刻：`data/mira_20260925_183000.csv`
  - 表头：`pc_time, seq, pico_time, CH4_ppm, H2O_ppm, C2H6_ppb`
  - 新增「打开数据文件夹」按钮

**v3：串口输入增强**
用户担心自动扫描出错、想保留手输。
- 澄清：`ttk.Combobox` 未设 `readonly`，**本来就能直接键盘输入**
- 加灰色提示「(可直接输入，如 COM7)」
- **记住上次成功连接的端口**（`data/last_port.txt`），刷新时不覆盖手输值
- 连接失败弹窗列出**所有可用端口 + 排查清单**（关 PuTTY / 查设备管理器 / 核对口号）

**v4：终端 / 远程控制标签页**
用户需求："能在不需要采集时输入指令 ssh 进 Pico 停止采集，避免直接关机的不良影响。"

新增第二个标签页：
- 一键远程按钮：停止采集 / 启动采集 / 查看状态 / 实时日志 / 重启 / **正常关机**（`sudo shutdown -h now`）
- 内嵌终端（cmd.exe 管道），可敲任意命令
- 「查网关 IP」辅助定位 Pico 地址
- 「免密设置说明」弹窗（ssh-keygen + authorized_keys + NOPASSWD）

**v5：布局修复**
- 横轴标签 `'时间 (s)'` → **`'Time (s)'`**（matplotlib 默认字体不含中文导致乱码）
- 原始数据区从 9 行文本框改为**固定高度容器**：190 → **280px**
- **右侧滚动边栏**（见困难 5）

---

## 十一、【困难 5】布局与显示异常

### 现象
用户反馈"原始数据区放大后没看到变化"。

### 根因分析（关键）
核算内容所需高度：
```
曲线区 ~570 + 顶部控制栏 30 + 原始数据区 280 + 底部按钮 30 ≈ 910px
窗口高度 = 760px
```
**内容本来就装不下。** Tkinter 在内容超出时会直接裁掉溢出部分或强行压缩控件——那 280px 的原始数据区很可能被挤出窗口外根本看不见。

这解释了为什么加大尺寸看不到效果。

### 解决：右侧滚动边栏
按用户要求"不要再调大小"，加入滚动容器：
- `tk.Canvas` + `ttk.Scrollbar` 包裹整个监测界面
- 所有控件放进可滚动的 `inner` 容器
- **滚轮滚动**：在界面任意位置（含曲线区）滚动即上下翻
- **智能区分**：鼠标在日志/输入框/下拉框内时只滚自身，不带动整页

效果：所有元素按各自设定尺寸完整渲染，不再被裁切。

---

## 十二、【困难 6】bat 启动脚本乱码

### 现象
```
'D:\.MyLearning\PyCharmProject\UAV_data_transaction\"' 不是内部或外部命令
'ion.py' 不是内部或外部命令
'鍏堢‘璁ゅ凡瀹夎渚濊禆锛?' 不是内部或外部命令
```

### 根因（两个叠加）
1. **换行符错误**：脚本用 Linux **LF** 生成，Windows 批处理必须 **CRLF**。cmd 按 LF 解析会错位拼接，导致 `mira_ground_station.py` 被拆成 `ion.py`、`o.` 等碎片
2. **编码错误**：UTF-8 中文在 GBK 终端下乱码

### 解决
- 换行符改 **CRLF**，编码改 **GBK**
- 提示文字全部改**英文**，彻底杜绝乱码
- 自动探测 `python` / `python3` / `py`
- **自动检查并安装** pyserial、matplotlib
- 路径加引号，支持含空格的目录

### 附带建议
用户使用 PyCharm，**直接 ▶️ 运行 .py 更省事**，报错信息完整、可断点调试。bat 仅作备用。

---

## 十三、开机自启动

### 为什么需要
手动 `python3 pico_ch4_bridge.py` 在 Pico 重启后不会自动回来。

### 实现（systemd）
```ini
[Unit]
Description=Pico to LoRa Bridge
After=multi-user.target

[Service]
Type=simple
User=debian
ExecStart=/usr/bin/python3 /home/debian/pico_ch4_bridge.py
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```
```bash
sudo systemctl daemon-reload
sudo systemctl enable pico-lora.service
sudo systemctl start pico-lora.service
```

### 关键澄清
**`systemctl stop` 不会影响开机自启。** 二者是独立开关：
- `enable` / `disable` → 永久开关
- `stop` / `start` → 只管当前这次
- `Restart=always` 只针对崩溃，显式 `stop` 不会被重新拉起

### 最重要的安全提醒
设置自启动后，**Pico 一开机 LoRa 即连续发射**，不再是偶尔发一帧。

> **开机前必须目视检查天线是否拧紧。** 不接天线连续发射是烧功放最快的方式。此前手动运行时的 `echo "HELLO"` 安全探针，在自启动模式下无法逐次执行，只能靠目视检查替代。

### 常见失败原因
`/dev/ttyUSB_LORA` 在服务启动时可能还不存在（设备识别晚）。有 `Restart=always` 会每 3 秒重试，通常几秒内自愈 —— **验证时务必等 1~2 分钟再判定失败**。

---

## 十四、交付物清单

| 文件 | 运行位置 | 说明 |
|---|---|---|
| `pico_ch4_bridge.py` | **Pico** | 读 RS232 数据口 → 提取 4 字段 → 加序号 → 写 LoRa 串口 |
| `mira_ground_station.py` | **Windows** | 地面站 GUI：三曲线 / 数值卡 / CSV / 丢包率 / 终端遥控 |
| `启动地面站.bat` | Windows | 一键启动（自动探测 Python、自动装依赖） |
| 本文 | — | 开发日志 |
| 《项目背景说明》 | — | 项目背景与硬件规格 |

---

## 十五、经验总结

### 三次方案级大改
| # | 触发 | 变更 | 决策依据 |
|---|---|---|---|
| 1 | 妙算 3 损坏 | 中间节点从妙算 3 → **Pico 自身** | Pico 本身就是 Debian 电脑，能承担"跑程序的中间节点"角色 |
| 2 | 端口识别混乱 | 数据源从猜测的 AERIS 口 → **实测的 RS232 口** | 验证靠 miniterm 直接看输出，不靠推理 |
| 3 | 单帧 134 字节超限 | 原样转发 → **提取 4 列重组短帧 + 加序号** | 既避开 121 上限，又让丢包率可统计 |

### 反复出现的坑（Top 5）
1. **天线必须先拧再上电** —— 贯穿全程，自启动后风险加倍
2. **端口漂移** —— 一律用别名，绝不写 ttyUSB0/1/2
3. **波特率不能动** —— 串口快于空速会撑爆缓冲区，且会误导排查
4. **串口不能双开** —— 跑地面站前必须关 PuTTY
5. **Windows 脚本必须 CRLF + GBK** —— LF 会导致命令被拆碎

### 方法论沉淀
- **先验证再开发**：每个阶段先用最小手段（echo、miniterm、dry-run）验证链路，再写正式代码
- **硬件问题优先用"对调法"定位**：收发角色对调能区分是发射坏还是接收坏
- **交叉验证**：串口无数据时用 Web 界面（:1880）确认仪器本身是否正常，能立刻区分"仪器坏"和"读不到"
- **dry-run 模式**：解析程序先加 `--dry-run` 只打印不发送，验证列索引正确后再正式运行

---

## 十六、尚未完成的工作

| 事项 | 说明 | 优先级 |
|---|---|---|
| **干扰测试** | 飞机开机 + 图传工作时测丢包率，与基线对比 | **最高** |
| 机载天线安装位置对比 | 贴机身 / 伸出机臂 / 朝下，选最优 | 高 |
| Pico 供电续航实测 | LoRa 从 Pico 取电，飞行中别掉电 | 高 |
| 实测拉距 | 地面端天线增益、高度对距离的影响 | 中 |
| udev 规则持久化验证 | 重启后 `ttyUSB_LORA` 是否稳定 | 中 |

### 干扰测试的判读标准（待执行）
| 丢包率 | 判断 | 处理 |
|---|---|---|
| 开机前后均 ~0% | 干扰可忽略 | 直接使用 |
| 开机后 10~30% | 有干扰但可用 | 图传锁 5.8GHz 后重测 |
| 开机后 >50% | 图传压制严重 | 锁 5.8G + 换信道 + 挪天线；仍不行需更换方案 |

> 测试时必须确保图传**真正工作**（遥控器已连、画面在跑），否则结果偏乐观。

### 备选方案（若 2.4GHz 干扰无法解决）
PSDK 提供 **MOP 通道**与 **Pilot 上云通道**，走 O4 图传链路是零硬件成本、零频段冲突的方案。建议 LoRa 定位为**独立于图传的备份链路**，而非唯一链路。

---

*日志结束 —— 当前状态：地面站已上线，无线链路与数据采集链路均已打通，待完成干扰测试后进入飞行验证阶段。*
