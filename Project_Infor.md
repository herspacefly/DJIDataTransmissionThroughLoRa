# 甲烷浓度无人机无线远距离传输 —— 项目背景说明

> 本文档供 AI 助手快速理解项目全貌。
> 阅读后应能判断：用户要做什么、已做到哪一步、有哪些硬性约束不能违反。

---

## 一、一句话目标

**让无人机在飞行中采集的甲烷浓度数据，实时、无线、远距离地传回地面电脑。**

---

## 二、业务背景

- 使用大疆 **Matrice 400（M400）** 行业无人机搭载气体监测设备，在空中采集甲烷浓度。
- 采集设备为 **Aeris Technology 公司的 Mira Pico** 气体监测设备。
- 应用场景为远距离巡检，因此地面端与无人机之间**不可能靠 WiFi 或有线连接**，需要一条独立的远距离无线数据链路。
- 数据量小：甲烷浓度 + 位置 + 时间戳，**每秒几十字节**即可满足需求，2Hz 更新频率已足够。

---

## 三、硬件清单与连接拓扑

### 硬件清单

| 设备 | 说明                                               |
|---|--------------------------------------------------|
| 大疆 Matrice 400（M400） | 行业无人机平台                                          |
| 大疆 妙算 3（Manifold 3） | 机载算力模块，运行 Ubuntu 20.04，通过 E-Port V2 装在飞机上        |
| 绿联 USB-C 拓展坞 | 插在妙算 3 唯一的 USB-C 口上，用于扩展外设                       |
| USB 无线网卡 | 插在拓展坞上，用于让妙算 3 连上 Pico 的 WiFi                    |
| Aeris Mira Pico | 甲烷气体监测设备，自带 WiFi 热点                              |
| 亿佰特 E28-2G4TBM-01 测试套件 ×2 | 2.4GHz LoRa 无线串口模块评估板（板载 E28-2G4T20SX），一块机载、一块地面 |
| 地面电脑 | Windows，用于接收数据                                   |

### 连接拓扑

```
【空中 - 无人机端】

  Mira Pico ──(WiFi)──> 妙算3的USB无线网卡
                              │
                              ↓
                          妙算 3 (Ubuntu 20.04)
                              │
                              │ USB-C → 绿联拓展坞
                              │              ├── USB无线网卡（连Pico）
                              │              └── E28 模块（/dev/ttyUSB0）
                              ↓
                        E28 机载端 ──┐
                                     │
                                     │  2.4GHz LoRa 无线
                                     │  （点对点广播，透明传输）
                                     │
                        E28 地面端 ──┘
                              │
                              │ USB (虚拟COM口)
                              ↓
【地面】              地面电脑 (Windows)
```

### 已实现的数据链路（Pico → 妙算 3）

1. 妙算 3 通过拓展坞上的 USB 无线网卡，连上 Mira Pico 的 WiFi 热点
2. 妙算 3 通过 **SSH** 从 Pico 取回甲烷浓度数据
3. **这一段已经跑通，不是当前要解决的问题**

### 正在实施的数据链路（妙算 3 → 地面电脑）★本项目核心

即上图中 E28 机载端 → 2.4GHz 无线 → E28 地面端 → 电脑 这一段。链路探测脚本已完成并完成一轮端到端收发；下一步是在无人机与图传实际工作条件下完成可复现的抗干扰验证，并接入甲烷业务数据。

---

## 四、选定的技术方案：亿佰特 E28-2G4T20SX

### 关键参数（出厂默认值，测试中未修改）

| 参数 | 值 |
|---|---|
| 工作频段 | 2400–2500 MHz |
| 默认信道 | 0x13 = **2413 MHz** |
| 串口波特率 | **9600**（8N1，无流控） |
| 空中速率 | **10 kbps** |
| 模块地址 | 0x0000 |
| 发射功率 | 20 dBm（可调 20/17/14/10） |
| 调制方式 | LoRa / FLRC / GFSK |
| 工作模式 | 模式 0（透明传输） |
| 单帧上限 | **121 字节**（普通模式） |
| 缓冲区 | 2048 字节 |
| 发射电流 | 160 mA @5V |
| 通信电平 | **3.3V TTL**（接 5V 会烧毁） |

### 模块配置命令（HEX 格式，PuTTY 等纯文本终端发不了）

| 命令 | 作用 |
|---|---|
| `C1 C1 C1` | 读取当前参数（返回 6 字节，如 `C0 00 00 13 18 04`） |
| `C0` + 5 字节参数 | 写入参数并掉电保存 |
| `C2` + 5 字节参数 | 写入参数，不保存 |
| `C3 C3 C3` | 读版本号 |
| `C4 C4 C4` | 复位模块 |
| `E2 E2 E2` / `E3 E3 E3` | 透传模式下开 10 秒配置窗口 |

---

## 五、妙算 3（机载端）环境

| 项目 | 值 |
|---|---|
| 系统 | Ubuntu 20.04，Linux Kernel 5.10 |
| SDK | JetPack 5.1.3，DJI PSDK 3.12 |
| Python | 3.8（`python3`） |
| SSH 账号 | `dji` / `dji` |
| **权限** | **无 root**，装包必须用 `pip3 install --user` |
| Python 串口依赖 | `pyserial 3.5` 已安装在用户目录 |
| E28 串口与权限 | `/dev/ttyUSB0`；`dji` 已在 `dialout` 组，可读写 |
| 测试脚本目录 | `/home/dji/Data_transaction` |
| USB-C 口 | **只有一个**，默认 5V/2A 输出，所有外设都靠拓展坞扩展 |

### SSH 地址

使用 `ssh dji@192.168.42.140` 指令即可进入妙算3，密码是dji
进入妙算3后，使用 `ssh debian@192.168.8.1` 指令即可进入PICO，密码是temppwd，PICO的WiFi密码是AerisTechnologies

---

## 六、已完成的工作（时间线）

| 步骤 | 状态 |
|---|---|
| Pico → 妙算 3 数据获取（WiFi + SSH） | 早前已完成 |
| 安装 CH340/CP210x 驱动（Windows 地面端） | 免装，系统自带 |
| 两块 E28 板分别在两台电脑上通过 PuTTY 互发 "HELLO" | **成功** |
| 妙算 3 识别 E28 模块 | `lsusb` 显示 `Bus 001 Device 005: ID 1a86:7523 QinHeng Electronics HL-340 USB-Serial adapter` |
| 链路探测脚本 | `beacon_tx.py` 与 `beacon_rx.py` 已完成，机载端已部署至 `/home/dji/Data_transaction` |
| 端到端链路探测 | `beacon_rx_20260925_160231.csv`：120 / 120 唯一有效帧，丢包率 0%，最大连续丢包 0 |
| 无人机开机、图传工作条件下的干扰测试 | **待完成**；现有 CSV 未记录测试工况，不能据此得出图传干扰结论 |

### PuTTY 配置要点（地面端已验证可用）

- Connection type: **Serial**
- Serial line: 对应 COM 口；Speed: **9600**
- Connection → Serial: 8 / 1 / None / Flow control **None**
- Terminal → Local echo: **Force on**（否则打字不显示）

---

## 七、已知风险与限制

### 1. 2.4GHz 频段与 M400 图传冲突

- M400 的 **O4 图传行业增强版工作频段包含 2.400–2.4835 GHz**
- E28 工作频段 2400–2500 MHz，默认信道 **2413 MHz 正落在图传带内**
- 同机载 + 同频段 + 图传功率远高于 20dBm，可能导致机载端 LoRa 被阻塞

**缓解手段（按优先级）：**
1. 在 DJI Pilot 2 中把图传已经**手动锁定到 5.8GHz**，把 2.4G 让给 LoRa
2. 更换 E28 信道，避开图传常用频点
3. 开启 LBT（发前侦听信道）

### 2. 妙算 3 的 USB-C 高压输出禁止开启

`DjiPowerManagement_OutputHighPower` 可输出 14V/5A，**会烧毁 USB 外设**（含 E28 板和 WiFi 网卡）。严禁调用。

### 3. 供电预算

USB-C 默认对外 5V/2A，拓展坞上挂 WiFi 网卡 + E28（发射瞬时 160mA），需留意 over-current 掉口。

### 4. 天线必须先接再上电

不接天线发射会导致反射功率烧毁功放。**顺序：拧好天线 → 插 USB；换天线 → 先断电。**
功放烧毁是隐性的（模块仍能识别、仍能接收，只是发不出去）。

### 5. 两块模块距离不能太近

小于 1 米会因接收机饱和出现"近距堵死"。**测试时保持 2~3 米以上。**

### 6. 环境干扰源

- **USB 3.0 接口**：其扩频时钟噪声落在 2.4GHz，会抬高接收底噪 → **优先用黑色 USB 2.0 口或加延长线**
- 金属桌面/机壳、天线附近有金属物体会严重衰减信号
- 两天线极化方向需一致（都竖直朝上）

---

## 八、当前状态

**无线链路已完成一次端到端程序化验证。** `beacon_tx.py` 在妙算 3 侧发送，Windows 侧 `beacon_rx.py` 收到完整的 120 帧；接收记录位于 `beacon_rx_20260925_160231.csv`，结果为 0% 丢包、最大连续丢包 0。

**妙算 3 的 E28 串口已具备程序读写条件。** 已确认 `/dev/ttyUSB0` 存在，`dji` 属于 `dialout` 组，且 `pyserial 3.5` 已安装。

尚未验证的是：

1. **无人机开机、遥控器连接且图传实际工作时，2.4GHz 干扰到底有多严重**
2. Pico 当前甲烷数据获取命令的输出格式、单位、位置字段与时间字段的正式契约；正式转发程序需要以该契约解析数据

---

## 九、下一步计划

### 阶段 1：补齐干扰测试（当前）

`beacon_tx.py` 与 `beacon_rx.py` 已可用，默认以 2 Hz 发送 60 秒、预期 120 帧。接收端会保存原始 CSV，统计唯一有效帧数、丢包率与最大连续丢包段，并在串口断连时重连。

**测试方法（对比法）：**
1. **基线**：飞机不开机（或妙算 3 独立供电），在固定距离、天线方向和模块参数下至少测 3 轮。
2. **干扰**：飞机上电、遥控器已连接且图传画面实际工作（不解锁、不起飞），保持相同条件至少测 3 轮。
3. 对比判读：

| 条件 | 判断 | 处理 |
|---|---|---|
| 丢包率 `<= 1%`、较基线增加 `<= 1` 个百分点，且最大连续丢包不超过 1 帧 | 正常 | 记录为可用基线，继续后续开发与复测。 |
| 丢包率 `(1%, 5%]`，或较基线增加 `(1, 5]` 个百分点，或连续丢失 2-3 帧 | 有干扰 | 保持条件复测；调整天线间距、安装位置和极化方向。 |
| 丢包率 `> 5%`，或较基线增加 `> 5` 个百分点，或连续丢失至少 4 帧 | 严重干扰 | 图传锁定 5.8 GHz、调整 E28 信道和天线位置后重新建立基线。 |

4. 可选：飞机开机状态下，对比机载天线不同安装位置（贴机身 / 伸出机臂 / 朝下），选最优

> 测试时务必让图传真正工作（遥控器已连、画面在跑），否则结果偏乐观。

### 阶段 2：正式程序开发（可启动）

1. **机载端转发程序**（妙算 3）：从 Pico 取甲烷浓度 → 封装短帧 → 串口发送（2Hz）
   - 建议帧格式：`$CH4,序号,浓度,纬度,经度,高度,采样时间戳\n`；字段单位、缺失值表示和 Pico 原始输出映射须先确认
   - 要求：串口断开自动重连、异常不崩、日志记录
2. **地面端接收程序**（Windows）：解析帧 → 实时显示 → 存 CSV → 画实时曲线
3. **开机自启动**：`systemd --user` 配置

阶段 2 可以先实现不依赖 Pico 最终字段定义的串口重连、帧封装、接收、CSV 落盘与显示骨架；在接入真实数据前，必须提供一条或多条 Pico 原始输出样本，并确认字段语义与单位。

---

## 十、给 AI 助手的硬性约束

1. **波特率固定 9600，不要提高。** 空中速率是 10kbps（约 1250 字节/秒），串口 9600（约 960 字节/秒）刚好匹配。若串口提到 115200 而空速不变，数据进得来发不出去，2048 字节缓冲区瞬间溢出，会疯狂丢包并误导排查方向。
   - 若确实需要提速，**串口波特率与空中速率必须同步提升**。
2. **单帧不超过 121 字节。** 不要发 JSON，用短 ASCII 帧。
3. **无 root 权限。** Python 包用 `pip3 install --user`；系统配置用 `systemd --user`。
4. **目标平台是 aarch64 Ubuntu 20.04 / Python 3.8**，代码需兼容。
5. **不要设计需要 ACK 握手的协议。** LoRa 透明传输是广播式，无确认重传机制；丢包统计只能靠序号连续性。
6. **严禁调用妙算 3 的高压输出接口。**
7. 数据帧建议自带**序号和时间戳**，便于丢包统计与事后分析。
