# 甲烷浓度无人机无线传输 —— 当前方案说明与操作手册

> 本文面向**实际操作者**，说明当前这套系统怎么一步步用起来，以及它为什么能工作。
> 配套文档：《甲烷监测无人机无线传输项目背景说明》（硬件规格）、《开发日志》（开发过程与踩坑）

---

# 第一部分：这套系统是什么

## 1.1 一句话概括

**Pico 一边采集甲烷，一边自己把数据通过 LoRa 无线电发给地面电脑；地面电脑用专用软件接收、画图、存盘。**

全程不需要人操作命令行，也不需要长按电源键关机。

## 1.2 系统拓扑

```
┌─────────────── 空中（无人机上）────────────────┐
│                                                 │
│   Mira Pico 气体监测仪                          │
│   ├─ 内部：传感器采集 → 生成数据文本             │
│   ├─ 内部：pico_ch4_bridge.py（开机自动运行）    │
│   │        读数据 → 提取4个字段 → 加序号          │
│   └─ USB ──> E28 LoRa 模块（机载端）             │
│                    │                            │
└────────────────────┼────────────────────────────┘
                     │
              2.4GHz 无线电波
           （点对点广播，透明传输）
                     │
┌────────────────────┼────────────────────────────┐
│                    ↓                            │
│   E28 LoRa 模块（地面端）                        │
│            │ USB（虚拟 COM 口）                  │
│            ↓                                    │
│   Windows 电脑                                   │
│   └─ mira_ground_station.py（地面站）            │
│        ├─ 实时显示 CH4 / H2O / C2H6              │
│        ├─ 三条实时曲线                           │
│        ├─ 自动存 CSV 到 data/ 文件夹             │
│        ├─ 统计丢包率                             │
│        └─ 终端标签页：远程停止采集 / 正常关机     │
│                                                 │
└─────────────── 地面 ────────────────────────────┘
```

## 1.3 三个关键角色

| 角色 | 设备 | 干的事 |
|---|---|---|
| **采集 + 发送** | Mira Pico + E28 机载端 | 采集气体、组帧、发射 |
| **无线传输** | 两块 E28 模块 | 把串口数据变成无线电波，再变回串口数据 |
| **接收 + 记录** | Windows 电脑 + 地面站 | 收数据、显示、画图、存盘、远程控制 |

---

# 第二部分：通信原理

## 2.1 核心思想：把"无线电"伪装成"一根串口线"

这是整个方案能这么简单的基础。

E28 模块是**透明传输**的：对 Pico 来说，它就是一个普通串口 `/dev/ttyUSB_LORA`；对电脑来说，就是一个普通 COM 口。

```
Pico 程序以为自己在写串口  ──>  实际变成了无线电波
电脑程序以为自己在读串口  <──  实际收到了无线电波
```

**两个程序都不知道无线的存在。** 它们只是在读写串口，无线电的部分全靠 E28 模块自己完成。

这就是为什么代码能这么短——不需要任何网络协议、不需要 socket、不需要处理连接断开重连。

## 2.2 完整数据旅程（一帧数据的十步）

以一帧数据为例，看它从传感器到屏幕经历了什么：

**① 传感器采集**
Pico 内部传感器测量甲烷浓度，同时输出水汽 H2O、乙烷 C2H6 作为辅助量。

**② Pico 生成原始数据**
Pico 内部把结果写成一行文本，从它的 RS232 串口输出（约 134 字节，1 秒一行）：

```
09/25/2026 18:28:59, ... ,1.923,1.05,0.0021,No Data,No Data
```

**③ 桥接程序读取**
`pico_ch4_bridge.py` 在 Pico 上常驻，从 `/dev/ttyUSB_RS232` 读这行文本。

**④ 提取 + 重组**
原始行有 134 字节，超过 LoRa 单帧上限 121 字节，直接发会丢包。程序只提取需要的 4 列：

- 第 1 列：时间戳
- 第 7 列：CH4（甲烷）
- 第 8 列：H2O（水汽）
- 第 9 列：C2H6（乙烷）

（末尾的 `No Data` 是无用数据，丢弃）

**⑤ 加序号**
重组时额外加一个自增序号。这步很关键，下面单独讲。

**⑥ 写串口 → 变成无线电**
程序把短帧写进 `/dev/ttyUSB_LORA`：

```
$MIRA,123,09/25/2026 18:28:59,1.923,1.05,0.0021
```

E28 模块收到串口字节，自动调制成 2.4GHz 无线电波发射出去。

**⑦ 空中传播**
地面端的 E28 模块接收到无线电波，解调还原成字节。

**⑧ 变成电脑的 COM 口数据**
地面 E28 通过 USB 把字节送给电脑，电脑以为"有个串口设备发数据来了"。

**⑨ 地面站读取**
地面站从 COM 口读到这一行，识别 `$MIRA` 帧头。

**⑩ 解析、显示、存盘**
拆出 4 个字段 → 更新数值卡 → 追加到曲线 → 写进 CSV 文件 → 用序号算丢包率。

## 2.3 为什么要加序号

**这是理解这套系统的关键设计。**

LoRa 透明传输是**广播式**的——像广场上有人喊话，喊完就完了，**没人会回答"收到了"**。所以发送端永远不知道数据有没有到。

没有序号的话，地面站只能看到"收到了 87 帧"，但不知道本该收到多少，也就**算不出丢包率**。

加了序号之后：

```
地面站收到：#1, #2, #3, #5, #6, #8 ...
                ↑ 缺 #4      ↑ 缺 #7

应到 = 最大序号 - 最小序号 + 1 = 100
实到 = 87
丢包率 = (100 - 87) / 100 = 13%
```

**这个丢包率数字非常重要**——它是判断"无人机图传有没有干扰我的链路"的唯一量化依据。做干扰测试时，飞机开机前后各测一次，对比丢包率就能知道干扰有多严重。

## 2.4 为什么是 9600 波特率（重要，不要改）

E28 有两个速度：

| 速度 | 值 | 说明 |
|---|---|---|
| 串口速率 | 9600 | 数据从程序进模块的速率 |
| 空中速率 | 10 kbps ≈ 1250 字节/秒 | 模块真正发送出去的速率 |

数据先进模块缓冲区，再由模块发出去。**串口速率 9600（约 960 字节/秒）慢于空中速率（约 1250 字节/秒）**，所以发得走，不会堆积。

如果把串口改成 115200，数据进得快、发得慢，2048 字节的缓冲区瞬间撑爆，就会疯狂丢包——**而且看起来和"被干扰"一模一样**，会严重误导排查。

> 甲烷数据量极小（约 45 字节/帧，1 秒 1 帧），9600 绰绰有余。**什么都不用调。**

## 2.5 为什么数据帧要这么短

| 限制 | 值 | 后果 |
|---|---|---|
| LoRa 单帧上限 | **121 字节** | 超了会丢包 |
| 空中速率 | 约 1250 字节/秒 | 帧越长，越占带宽、越易受干扰 |

原始行 134 字节 → 超限。重组后约 45 字节，**既避开上限，又省了三分之二带宽**，抗干扰能力也更强。

## 2.6 为什么是两块模块，不能只有一块

LoRa 是**点对点**的。一块只能做一端（发或收），必须两块参数一致才能互通。两块地址、信道相同，形成一对广播通道。

## 2.7 必须知道的三个风险

### ① 2.4GHz 与无人机图传冲突（最大风险）

M400 的 O4 图传工作频段包含 **2.400–2.4835 GHz**，与 E28 的 2400–2500 MHz **完全重叠**，默认信道 2413 MHz 正落在图传带内。

图传发射功率远高于 E28 的 20dBm，可能导致机载端被"压死"。

**缓解办法（按优先级）：**
1. 在 Pilot 2 里把图传**手动锁定到 5.8GHz**，把 2.4G 让给 LoRa
2. 更换 E28 信道
3. 开启 LBT（发前侦听信道）
4. 机载天线远离 O4 天线和碳板，最好伸出机身、朝下安装

> **这就是必须做干扰测试的原因。** 飞机停在地上就能测，见第五部分。

### ② 天线必须先接再上电

不接天线就发射，功率辐射不出去会**全部反射回功放管**，几秒到几分钟烧毁。

而且这种损坏是**隐性的**——模块还能被识别、还能接收，只是发不出去。会让你误判成"程序写错了"或"被干扰了"。

**顺序：拧好天线 → 插 USB；换天线 → 先断电。**

### ③ 两块模块不能贴太近

小于 1 米会因接收机饱和出现"近距堵死"——**距离太近反而收不到**。测试时保持 2~3 米以上。

---

# 第三部分：使用前的准备（只需做一次）

## 3.1 硬件连接

**机载端（Pico）：**
1. **先把胶棒天线拧到 E28 模块的 SMA 座上**（用手拧紧即可）
2. E28 插到 Pico 的 USB 口（通过拓展坞）

**地面端（电脑）：**
1. **同样先拧天线**
2. E28 用 Type-C 线插电脑 USB 口
3. 建议插**黑色 USB 2.0 口**（蓝色 USB 3.0 口的噪声正好落在 2.4GHz，会干扰接收）
4. 用 USB 延长线把模块拉离主机，放在窗边或空旷处

## 3.2 安装 Python 依赖（电脑端）

在 PowerShell 或 PyCharm 的 Terminal 里执行：

```powershell
pip install pyserial matplotlib
```

- `pyserial`：读写串口，必需
- `matplotlib`：画曲线，不装也能跑（只是没图）

## 3.3 配置 SSH 免密登录（一次性，之后永久生效）

### 为什么要做
地面站的「停止采集」「正常关机」按钮，本质是让电脑自动 SSH 到 Pico 执行命令。**如果每次都要输密码，自动化就会卡住。**

配好之后，点一下按钮就完成操作。

### 步骤一：生成密钥

在 Windows PowerShell 执行（一路回车，不要设密码）：

```powershell
ssh-keygen
```

### 步骤二：把公钥传给 Pico

先确认 Pico 的地址。电脑连上 Pico 的 WiFi 后，在 PowerShell 执行：

```powershell
ipconfig
```

找「默认网关」那一行，那就是 Pico 的 IP（假设是 `192.168.1.1`，用户名 `debian`）。

然后执行（把地址换成你的实际值）：

```powershell
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh debian@192.168.1.1 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

这步会要求输一次 Pico 的密码，输完就永久生效了。

### 步骤三：让 sudo 不再要密码

SSH 登录 Pico 后执行：

```bash
echo "debian ALL=(ALL) NOPASSWD:ALL" | sudo tee /etc/sudoers.d/debian
```

### 验证

回到 PowerShell，直接执行：

```powershell
ssh debian@192.168.1.1 "echo OK"
```

**不提示输密码、直接输出 OK = 配置成功。**

> 也可以直接点地面站里的「免密设置说明」按钮，同样内容会弹窗显示。

## 3.4 设置 Pico 开机自启动（一次性）

让 Pico 一开机就自动开始采集和发送，你什么都不用做。

SSH 登录 Pico 后：

```bash
sudo nano /etc/systemd/system/pico-lora.service
```

粘贴以下内容（用户名 `debian` 按实际改）：

```ini
[Unit]
Description=Pico to LoRa Bridge
After=multi-user.target

[Service]
Type=simple
User=debian
ExecStart=/usr/bin/python3 /home/debian/pico_ch4_bridge.py
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

保存退出后执行：

```bash
sudo systemctl daemon-reload
sudo systemctl enable pico-lora.service
sudo systemctl start pico-lora.service
```

**验证是否开启成功：**

```bash
systemctl is-enabled pico-lora    # 输出 enabled = 开机自启已开
systemctl is-active pico-lora     # 输出 active = 正在运行
```

### 安全提醒（必读）

设置自启动后，**Pico 一开机 LoRa 就开始连续发射**，不再是偶尔发一帧。

> **每次开机前，务必目视检查天线是否拧紧。** 不接天线连续发射是烧毁功放最快的方式。

---

# 第四部分：日常使用流程

> 前提：3.1 ~ 3.4 的准备工作已完成。

## 4.1 开机与开始采集

**① 检查天线**
机载端和地面端的两根天线都确认拧紧。**这是每次开机必做的动作。**

**② 开启 Pico**
开机后等待 1~2 分钟，让系统启动、桥接程序自动运行（systemd 会拉起来）。

**③ 打开地面站**
双击 `启动地面站.bat`，或在 PyCharm 里运行 `mira_ground_station.py`。

> ⚠️ **如果 PuTTY 开着，先关掉。** 串口不能同时被两个程序占用。

**④ 连接串口**
- 串口下拉框会自动扫描，选带 E28 的那个 COM 口（首次需确认，之后会记住）
- 波特率保持 **9600**
- 点「连接」

**⑤ 确认数据在跑**
看到以下现象即正常：
- CH4 / H2O / C2H6 数值卡在刷新
- 三条曲线在走
- 原始数据区每秒刷一行 `$MIRA,...`
- 状态栏显示速率约 1 帧/秒

**⑥ 确认存盘**
勾选「自动存 CSV」，数据会自动写进程序目录下 `data/` 文件夹，**每 30 分钟自动新建一个文件**。

点「打开数据文件夹」可随时查看。

## 4.2 飞行中

- **盯丢包率**：状态栏实时显示。正常应接近 0%
- 若丢包率突然飙升，可能是图传干扰或飞出通信距离
- 数据自动存盘，**无需任何操作**

## 4.3 结束采集（不关机，只是停发数据）

切到「**终端 / 远程控制**」标签页 → 点「**停止采集**」。

背后执行的是 `sudo systemctl stop pico-lora`。

**这不会影响开机自启动**——下次开机它还会自动运行。stop 只是停当前这次。

想重新开始采集，点「**启动采集**」。

## 4.4 关闭设备（收工）

**正确做法**：切到「终端」标签 → 点「**正常关机**」。

背后执行的是 `sudo shutdown -h now`，会正常同步文件系统后关机。

### 为什么不能长按电源键

Pico 是精密仪器，里面存着采集数据。**长按电源键强制断电**有小概率损坏文件系统。用命令关机是安全的。

### 关机顺序建议

1. 点「停止采集」（可选，停止发射）
2. 点「正常关机」
3. 等 Pico 完全关闭后，关闭地面站窗口

---

# 第五部分：必做的干扰测试

> **这一步还没做，但非常重要。** 飞机停在地上就能测，成本极低，却决定了整套方案在你的真机上是否可行。

## 为什么要做

M400 图传与 E28 共用 2.4GHz 频段（见 2.7）。现在实验室里测通了，不代表飞机开机、图传工作时还能通。

## 测试步骤

**① 基线测试（飞机不开机）**
- 地面站连上，记录 2 分钟
- 记下丢包率 → **应接近 0%**

**② 干扰测试（飞机开机）**
- 给飞机上电，**遥控器连上、图传画面在跑**（不解锁、不起飞）
- 再记录 2 分钟
- 记下丢包率

> ⚠️ 必须让图传**真正工作**。飞机开机但遥控器没连，测出来会偏乐观。

**③ 对比判读**

| 丢包率 | 判断 | 处理 |
|---|---|---|
| 开机前后均 ~0% | 干扰可忽略 | 直接使用 |
| 开机后 10~30% | 有干扰但可用 | 图传锁 5.8GHz 后重测 |
| 开机后 >50% | 图传压制严重 | 锁 5.8G + 换信道 + 挪天线；仍不行需更换方案 |

**④ 可选：天线位置对比**
飞机开机状态下，把机载天线换三个位置各测一遍（贴机身 / 伸出机臂 / 朝下），选丢包最低的。

**这个数据等你真飞起来就没法从容测了。**

---

# 第六部分：常见问题速查

| 现象 | 原因 | 解决 |
|---|---|---|
| 串口下拉框为空 | 驱动没装 / 线不能传数据 | 设备管理器查看；换一根确认能传数据的线 |
| 连接时"拒绝访问" | **PuTTY 还开着** | 关闭 PuTTY 后重试 |
| 连上但无数据 | COM 口选错 / 天线没接 | 核对 COM 口；检查天线 |
| 数据全是乱码 | 波特率不一致 | 两边都是 9600 |
| 丢包率很高 | 图传干扰 / 距离过远 / 波特率被改高 | 按第五部分测干扰；确认波特率未动 |
| 地面站报错 `No module named 'serial'` | 依赖没装 | `pip install pyserial` |
| 远程按钮无反应 | SSH 免密没配好 | 按 3.3 重新配置；用 `ssh debian@IP "echo OK"` 验证 |
| 提示找不到 ssh | Windows 没装 OpenSSH 客户端 | 设置 → 应用 → 可选功能 → 安装 OpenSSH 客户端 |
| 数值卡字段对不上 | Pico 列索引有偏差 | 改 `pico_ch4_bridge.py` 顶部 `IDX_TS, IDX_CH4, IDX_H2O, IDX_C2H6` |
| Pico 重启后不自动发 | systemd 未 enable | `sudo systemctl enable pico-lora`；设备识别慢时等 1~2 分钟 |
| 收不到但模块灯闪 | 功放可能已烧（隐性） | 收发角色对调验证；确认此前未空载发射 |

---

# 附录 A：关键参数速查

| 项目 | 值 |
|---|---|
| 串口波特率 | **9600**（不要改） |
| 数据位/停止位/校验 | 8 / 1 / None |
| 流控 | None |
| LoRa 工作频段 | 2400–2500 MHz（默认信道 2413 MHz） |
| 空中速率 | 10 kbps |
| 单帧上限 | **121 字节**（实际约 45 字节） |
| 发射功率 | 20 dBm |
| 数据帧格式 | `$MIRA,<序号>,<时间戳>,<CH4>,<H2O>,<C2H6>` |
| 数据速率 | 1 帧/秒，约 45 字节/秒 |
| CSV 轮转 | 每 30 分钟 |
| Pico 数据口 | `/dev/ttyUSB_RS232` |
| Pico LoRa 口 | `/dev/ttyUSB_LORA` |
| Pico 服务名 | `pico-lora` |

# 附录 B：交付文件

| 文件 | 运行位置 | 说明 |
|---|---|---|
| `pico_ch4_bridge.py` | **Pico** | 读数据口 → 提取 4 字段 → 加序号 → 写 LoRa |
| `mira_ground_station.py` | **Windows** | 地面站 GUI |
| `启动地面站.bat` | Windows | 一键启动（自动探测 Python、自动装依赖） |
| 《当前方案说明与操作手册》 | — | 本文 |
| 《项目背景说明》 | — | 硬件规格与背景 |
| 《开发日志》 | — | 开发过程与踩坑记录 |

# 附录 C：常用命令速查

**Windows（PowerShell）**
```powershell
ssh debian@192.168.1.1 "echo OK"      # 验证免密
ipconfig                               # 查 Pico 网关 IP
pip install pyserial matplotlib        # 装依赖
```

**Pico（SSH 后）**
```bash
systemctl status pico-lora             # 看服务状态
sudo systemctl stop pico-lora          # 停止采集（不影响开机自启）
sudo systemctl start pico-lora         # 启动采集
journalctl -u pico-lora -f             # 看实时日志
ls -l /dev/ttyUSB*                     # 查串口与别名
sudo shutdown -h now                   # 正常关机
```

---

*本文对应系统状态：地面站已上线，采集与无线链路已打通。**干扰测试尚未执行**，建议尽快完成。*
